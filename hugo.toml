# ---------------------------------------------------------------------------
# Antilo site using the pico-corp demo layout.
# Starter agency copy: see DEMO-CONTENT.md for remaining placeholders.
#
# This is also the file to copy when you start your own site: every parameter
# the theme reads is set here with a real value, so you can see what each one
# does rather than guess from a blank template.
#
# ---------------------------------------------------------------------------

baseURL = 'https://antilodesign.com/'
title = 'Antilo Consulting and Design'
theme = 'hugo-theme-pico-corp'
locale = 'en-US'
defaultContentLanguage = 'en'
timeZone = 'America/Denver'

enableRobotsTXT = true
summaryLength = 22
capitalizeListTitles = false

[pagination]
  pagerSize = 6

[taxonomies]
  category = 'categories'
  tag = 'tags'

[minify]
  minifyOutput = true

[imaging]
  quality = 78
  resampleFilter = 'Lanczos'
  hint = 'photo'

[markup]
  [markup.goldmark.renderer]
    unsafe = false
  [markup.highlight]
    noClasses = false
    lineNumbersInTable = false
    tabWidth = 2
  [markup.tableOfContents]
    startLevel = 2
    endLevel = 3

[services]
  [services.rss]
    limit = 20

# ---------------------------------------------------------------------------
# Navigation
# ---------------------------------------------------------------------------

[[menu.main]]
  name = 'Services'
  pageRef = '/services'
  weight = 10
[[menu.main]]
  name = 'Work'
  pageRef = '/work'
  weight = 20
[[menu.main]]
  name = 'About'
  pageRef = '/about'
  weight = 30
[[menu.main]]
  name = 'Journal'
  pageRef = '/blog'
  weight = 40
[[menu.main]]
  name = 'Contact'
  pageRef = '/contact'
  weight = 50

# Footer links are grouped by the `group` menu parameter.
[[menu.footer]]
  name = 'Website Design'
  pageRef = '/services/website-design'
  weight = 10
  [menu.footer.params]
    group = 'Services'
[[menu.footer]]
  name = 'Digital Marketing'
  pageRef = '/services/digital-marketing'
  weight = 20
  [menu.footer.params]
    group = 'Services'
[[menu.footer]]
  name = 'Print Marketing'
  pageRef = '/services/print-marketing'
  weight = 30
  [menu.footer.params]
    group = 'Services'
[[menu.footer]]
  name = 'SEO'
  pageRef = '/services/seo'
  weight = 40
  [menu.footer.params]
    group = 'Services'
[[menu.footer]]
  name = 'Local SEO'
  pageRef = '/services/local-seo'
  weight = 42
  [menu.footer.params]
    group = 'Services'
[[menu.footer]]
  name = 'Branding & Strategy'
  pageRef = '/services/branding'
  weight = 44
  [menu.footer.params]
    group = 'Services'
[[menu.footer]]
  name = 'About us'
  pageRef = '/about'
  weight = 50
  [menu.footer.params]
    group = 'Company'
[[menu.footer]]
  name = 'Project concepts'
  pageRef = '/work'
  weight = 60
  [menu.footer.params]
    group = 'Company'
[[menu.footer]]
  name = 'Journal'
  pageRef = '/blog'
  weight = 70
  [menu.footer.params]
    group = 'Company'
[[menu.footer]]
  name = 'Contact'
  pageRef = '/contact'
  weight = 80
  [menu.footer.params]
    group = 'Company'

[[menu.legal]]
  name = 'Privacy'
  pageRef = '/legal/privacy'
  weight = 10
[[menu.legal]]
  name = 'Business information'
  pageRef = '/legal/imprint'
  weight = 20

# ---------------------------------------------------------------------------
# Theme parameters
# ---------------------------------------------------------------------------

# Sub-tables such as [params.address] must come after every plain key in
# [params]: in TOML a table header captures all following keys until the next
# header, so a key written below [params.address] becomes params.address.<key>
# and the theme never sees it.
[params]
  description = 'Antilo is a Denver, Colorado marketing consultancy and design agency for website design, digital and print marketing, SEO, and local SEO.'

  # Company details. Nothing company-specific is hardcoded in the templates.
  companyName = 'Antilo'
  legalName = '' # Add the confirmed legal business name before launch.
  tagline = 'Denver design & marketing'
  # Add founded when the actual year is confirmed.
  email = '' # Add a confirmed public contact email.
  phone = ''
  phoneHref = ''
  vatId = ''
  openingHours = ''

  # Brand. primaryColor drives the generated brand stylesheet; primaryColorDark
  # is used when the dark scheme is active.
  primaryColor = '#34594C'        # Forest green: light-mode buttons and links
  primaryColorDark = '#A8C9B5'    # Light sage-green: dark-mode buttons and links
  radius = '0.5rem'
  # Display face for headings, statistics and pull quotes. 'serif' uses the
  # best serif already installed on the reader's system, which is what gives
  # the theme its editorial feel at zero network cost. 'sans' opts out.
  headingFont = 'serif'
  logo = ''            # path under assets/, e.g. 'images/logo.svg'. Empty = wordmark.
  logoDark = ''
  favicon = '/antelope_logo_nobg.svg'
  appleTouchIcon = '/antelope_logo_nobg.png'
  ogImage = ''      # Add an Antilo share image before launch.

  # Colour scheme: 'system', 'light' or 'dark'.
  defaultScheme = 'system'
  showSchemeToggle = true

  showBreadcrumbs = true
  showReadingTime = true
  showRelated = true

  [params.address]
    street = ''
    postalCode = ''
    city = 'Denver'
    region = 'Colorado'
    country = 'United States'
    countryCode = 'US'
    mapURL = ''

  [params.announcement]
    enabled = true
    text = 'Website design, marketing, and local visibility. Based in Denver.'
    linkLabel = 'Start a project'
    linkURL = '/contact/'

  # Site-wide fallback for the closing call to action.
  [params.cta]
    eyebrow = 'Next step'
    title = 'What is next for your business?'
    text = 'A new website, a clearer message, or a stronger local presence. Tell us what you have in mind, and let’s find a useful place to start.'
    primaryLabel = 'Start a conversation'
    primaryURL = '/contact/'
    secondaryLabel = 'Explore our services'
    secondaryURL = '/services/'

  [params.footer]
    text = 'Antilo is a Denver, Colorado design and marketing agency. Websites, digital and print marketing, SEO, and local SEO—with a clear purpose.'
    note = 'Working preview: project concepts and photography are placeholders.'

  [params.seo]
    titleSeparator = '·'
    twitterSite = ''
    twitterCreator = ''
    # Types: Organization, LocalBusiness, ProfessionalService …
    organizationType = 'ProfessionalService'

  # Leave both empty to ship zero third-party requests.
  [params.analytics]
    plausibleDomain = ''
    googleAnalyticsID = ''

  # Point action at Formspree, Netlify Forms or your own endpoint.
  [params.contactForm]
    action = ''
    method = 'POST'
    netlify = false
    successMessage = 'Thanks for getting in touch with Antilo.'

  [[params.social]]
    name = 'RSS'
    icon = 'rss'
    url = '/index.xml'
